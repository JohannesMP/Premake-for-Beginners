#ignore temporary os files
Thumbs.db
*~
.DS_Store


# don't ignore directories, so we can recurse into them
!*/

# Don't ignore dummy files, which are used when laying out folders
# - Git won't commit empty folders
!*.gitkeep
!*/*.gitkeep

# Do not commit bin, make or project folders generated by premake/cmake/doxygen
bin/
bin_*/
make/
project_*/
documentation/

